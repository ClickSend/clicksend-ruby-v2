source 'https://rubygems.org'

gemspec

group :development, :test do
  gem 'rake', '~> 13.0.1'
  gem 'pry-byebug'
  gem 'rubocop', '~> 0.66.0'
  gem 'ostruct' # no longer a default gem as of Ruby 3.5+/4.0; rake requires it internally
end
